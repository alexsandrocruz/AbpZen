/* This file is automatically generated by ABP framework to use MVC Controllers from javascript. */


// module payment

(function(){

  // controller volo.payment.requests.paymentRequest

  (function(){

    abp.utils.createNamespace(window, 'volo.payment.requests.paymentRequest');

    volo.payment.requests.paymentRequest.complete = function(paymentMethod, parameters, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/' + paymentMethod + '/complete',
        type: 'POST',
        data: JSON.stringify(parameters)
      }, ajaxParams));
    };

    volo.payment.requests.paymentRequest.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/requests',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    volo.payment.requests.paymentRequest.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/requests/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    volo.payment.requests.paymentRequest.handleWebhook = function(paymentMethod, payload, headers, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/' + paymentMethod + '/webhook' + abp.utils.buildQueryString([{ name: 'payload', value: payload }]) + '',
        type: 'POST',
        headers: {
          'headers': headers
        }
      }, ajaxParams));
    };

    volo.payment.requests.paymentRequest.start = function(paymentMethod, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/' + paymentMethod + '/start',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

  })();

  // controller volo.payment.plans.plan

  (function(){

    abp.utils.createNamespace(window, 'volo.payment.plans.plan');

    volo.payment.plans.plan.getGatewayPlan = function(planId, gateway, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/plans/' + planId + '/' + gateway + '',
        type: 'GET'
      }, ajaxParams));
    };

    volo.payment.plans.plan.getPlanList = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/plans',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller volo.payment.gateways.gateway

  (function(){

    abp.utils.createNamespace(window, 'volo.payment.gateways.gateway');

    volo.payment.gateways.gateway.getGatewayConfiguration = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/gateways',
        type: 'GET'
      }, ajaxParams));
    };

    volo.payment.gateways.gateway.getSubscriptionSupportedGateways = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/payment/gateways/subscription-supported',
        type: 'GET'
      }, ajaxParams));
    };

  })();

})();


