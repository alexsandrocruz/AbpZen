@page "{id}"
@using Microsoft.AspNetCore.Mvc.Localization
@using Volo.Abp.AspNetCore.Mvc.UI.Bootstrap.TagHelpers.Modal;
@using Sapienza.Lexus
@using Sapienza.Lexus.Localization
@inject IHtmlLocalizer<Sapienza.LexusResource> L
@model Sapienza.Lexus.Web.Pages.Lawyer.EditModel

@section scripts {
    <script>
        var lawyerSpecializationInitialData = @Html.Raw(Json.Serialize(Model.ViewModel.LawyerSpecializations));
    </script>
    <abp-script src="/Pages/Lawyer/Edit.js" />
}





<form method="post" id="EditLawyerForm">
    <abp-card>
        <abp-card-header>
            <abp-card-title>@L["EditLawyer"].Value</abp-card-title>
        </abp-card-header>
        <abp-card-body>
            <abp-tabs>
                <abp-tab title="@L["General"].Value">
                    
    <input type="hidden" asp-for="Id" />
    <abp-row>
        <abp-column size-md="_12">
            <abp-input asp-for="ViewModel.FullName" label="@L["Lawyer:FullName"].Value" />
        </abp-column>
        <abp-column size-md="_12">
            <abp-input asp-for="ViewModel.PreferredName" label="@L["Lawyer:PreferredName"].Value" />
        </abp-column>
    </abp-row>

                </abp-tab>
                <abp-tab title="@L["Specializations"].Value">
                    <div class="mt-2">
                        <div class="d-flex justify-content-between align-items-center mb-3">
                            <h6 class="mb-0">@L["Specializations"]</h6>
                            <button type="button" class="btn btn-sm btn-outline-primary" id="AddLawyerSpecializationBtn">
                                <i class="fa fa-plus"></i> @L["Add"]
                            </button>
                        </div>
                        <table class="table table-striped table-hover table-bordered w-100" id="LawyerSpecializationsTable">
                            <thead class="table-light">
                                <tr>
                                    <th style="width: 80px">@L["Actions"]</th><th>@L["LawyerSpecialization:LawyerId"]</th><th>@L["LawyerSpecialization:SpecializationId"]</th>
                                </tr>
                            </thead>
                            <tbody></tbody>
                        </table>
                    </div>
                </abp-tab>
            </abp-tabs>
        </abp-card-body>
        <abp-card-footer>
            <a asp-page="Index" class="btn btn-secondary">@L["Cancel"]</a>
            <abp-button button-type="Primary" type="submit" text="@L["Save"].Value" />
        </abp-card-footer>
    </abp-card>
</form>
